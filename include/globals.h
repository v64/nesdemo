#include "constants.h"
#include "registers.h"

extern unsigned char cpu_memory[CPU_MEMORY_SIZE];
extern unsigned char ppu_memory[PPU_MEMORY_SIZE];
extern struct cpu_regs_list cpu_regs;
extern unsigned char cpu_flags;
